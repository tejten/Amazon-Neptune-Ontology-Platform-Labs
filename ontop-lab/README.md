# Ontop Lab Files

These files create a tiny Ontop virtual knowledge graph over a PostgreSQL RMS-style table.

Use them with:

```bash
docker compose up -d
```

The Ontop SPARQL endpoint runs at:

```text
http://<host>:8080/sparql
```

See:

```text
labs/08a-create-ontop-endpoint.md
```

